fn main() {
    // functions which end with "!" are not exactly functions, but macros
    // when this code is compiled, macros are expanded into the actual rust code
    println!("Hello, world!");
}
